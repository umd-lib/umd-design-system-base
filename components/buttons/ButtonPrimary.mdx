import {
  Meta,
  Canvas,
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
  ArgTypes,
  Source,
} from "@storybook/blocks";

import * as ButtonPrimaryStories from "./ButtonPrimary.stories";

<Meta of={ButtonPrimaryStories} />

<Title />
<Subtitle />
<Description />
<Primary />
<Controls />

### Note

Currently, the disabled status won’t be included in the UMD Libraries Design System.

### Features

There are only 2 types of button will be used in the design system, which are:

- Primary
- Secondary

The primary button serves as the main action button, while the secondary button is used for "link to more" functions.

### Twig

#### Primary Button

```twig
<button class="t-body-small t-interactive-sub button--primary c-content-interactive-primary c-bg-interactive-primary ani-background">{{ text }}</button>
```

<Stories />
