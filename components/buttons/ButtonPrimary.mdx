import {
  Meta,
  Canvas,
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
  ArgTypes,
  Source,
} from "@storybook/blocks";

import * as ButtonPrimaryStories from "./ButtonPrimary.stories";

<Meta of={ButtonPrimaryStories} />

<Title />
<Subtitle />
<Description />
<Primary />
<Controls />

### Implementation Note

Currently, the disabled status won’t be included in the UMD Libraries Design System.

### Features

There are only 2 types of button will be used in the design system, which are:

- Primary - main call-to-action(hero section etc)
- Secondary - components level action(link to articles etc))

Both variants function as styled links rather than traditional buttons. Disabled states are not supported.

### Text Fields

<table>
  <tr>
    <th>Variant</th>
    <th>Maximum Characters</th>
  </tr>
  <tr>
    <td>Primary Button</td>
    <td>25</td>
  </tr>
  <tr>
    <td>Secondary Button</td>
    <td>50</td>
  </tr>
</table>

### Twig

#### Primary Button

```twig
<a href={{ url }}class="t-body-small t-interactive-sub button--primary c-content-interactive-primary c-bg-interactive-primary ani-background">{{ text }}</a>
```
